import 'package:flutter/material.dart';

class MusicRecorder extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Music Recorder')),
      body: Center(
        child: Text('Music Recorder Screen'),
      ),
    );
  }
}
