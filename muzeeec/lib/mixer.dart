import 'package:flutter/material.dart';

class Mixer extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Mixer')),
      body: Center(
        child: Text('Mixer Screen'),
      ),
    );
  }
}
